diff --git a/my.clearbanc.com/jest.setup.js b/my.clearbanc.com/jest.setup.js
index cd5dcffc63..ecc7b3eb19 100644
--- a/my.clearbanc.com/jest.setup.js
+++ b/my.clearbanc.com/jest.setup.js
@@ -1,25 +1,22 @@
-import Vue from 'vue';
 import { createI18n } from 'vue-i18n';
 import messages from '@/lang/';
 import { config } from '@vue/test-utils';
-// Vue-Testing-Library
 import { configure } from '@testing-library/vue';
 
-// Stops Warning Error
-// [Vue warn]: Unknown custom element: <icon> - did you register the component correctly?
-// For recursive components, make sure to provide the "name" option.
-Vue.config.silent = true;
-
 export const i18n = createI18n({
   locale: process.env.VUE_APP_I18N_LOCALE || 'en-us',
   fallbackLocale: process.env.VUE_APP_I18N_FALLBACK_LOCALE || 'en-us',
   messages,
 });
 
-config.mocks.i18n = i18n;
+config.global.mocks.i18n = i18n.global;
+
+// config.global.plugins = [i18n];
+
+config.global.provide.i18n = i18n.global;
 
-config.mocks.$t = (key, optionalParams) => {
-  return i18n.t(key, optionalParams);
+config.global.mocks.$t = (key, optionalParams) => {
+  return i18n.global.t(key, optionalParams);
 };
 
 // Add env vars needed for tests
@@ -32,4 +29,11 @@ process.env = Object.assign(process.env, {
 // Vue-Testing-Library Set up
 configure({
   testIdAttribute: 'data-cy',
+  global: {
+    mocks: {
+      $t: (key, optionalParams) => {
+        return i18n.global.t(key, optionalParams);
+      },
+    },
+  },
 });
